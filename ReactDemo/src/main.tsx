import React from 'react'
import "./index.css"
import ReactDOM from 'react-dom/client'
import TailwindComponents from './pages/TailwindComponents'

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <TailwindComponents/>
    </React.StrictMode>,
)
